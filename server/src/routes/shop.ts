import express from 'express';

/**
 * Routes giving possible items, staff or constructions than *could* be bought
 */

////////////////////////////////////////////////////////////////////////////////

const router = express.Router();

////////////////////////////////////////////////////////////////////////////////

router.get( '/shop/lands', ( request, response ) =>
{
	response.status( 200 ).send( 'Get shop lands' );
});

////////////////////////////////////////////////////////////////////////////////


export default router;
