import express from 'express';

/**
 * Routes for creating, managing et retrieving token on the account
 */

////////////////////////////////////////////////////////////////////////////////

const router = express.Router();

////////////////////////////////////////////////////////////////////////////////

router.get( '/auth/create', ( request, response ) =>
{
	response.status( 200 ).send( 'want to create account' );
});

////////////////////////////////////////////////////////////////////////////////

export default router;
